básico: clone, checkout, add, commit, push

outros: merge, rebase, status, reset, diff, branch, log, rm,
mv, stach.blame, cherry pick, fetch, show

locais: diretório local, staging, local repository e remote repository
diretório local -> arquivos
staging -> local temporário onde serão armazenadas as mudanças antes de
serem clommitadas
local repository -> armazena as mudanças que já foram commitadas localmente

git pull = git fetch + git marge
comando ckechout: permite mudar as branches

